// Local-dev/build-time stub. Vite copies this into dist/ as a static
// asset; the Docker entrypoint (docker-entrypoint.sh) overwrites it at
// container start with real environment values. Empty here means
// authConfig.js falls back to its non-prod default, same as local dev.
window.__APP_CONFIG__ = {};
